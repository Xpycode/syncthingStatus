<!-- DRAFT appcast item for v1.6.1 — move into /appcast.xml ONLY after the GitHub release
     and DMG exist. Fill in: length (bytes, from make-dmg.sh output), sparkle:edSignature
     (from sign_update), and the real pubDate. -->

        <item>
            <title>Version 1.6.1</title>
            <description><![CDATA[
                <h2>What's New in Version 1.6.1</h2>
                <h3>Bug Fixes</h3>
                <ul>
                    <li><strong>Works with "Use HTTPS for GUI"</strong> &mdash; if Syncthing's GUI was set to HTTPS (which also silently redirects the default <code>http://127.0.0.1:8384</code> there), the app could not connect at all: macOS rejected Syncthing's self-signed certificate with an alarming "server that is pretending to be" error. The app now trusts Syncthing's certificate <strong>for local connections only</strong> (localhost / 127.x.x.x / ::1). Connections to a remote Syncthing over HTTPS still fail closed &mdash; but now with a clear message naming the setting instead of Apple's scary wording.</li>
                    <li><strong>About panel no longer claims "Syncthing: not connected"</strong> while fully connected &mdash; a background refresh being superseded could wipe the remembered Syncthing version; it now survives cancelled refreshes.</li>
                </ul>
            ]]></description>
            <pubDate>REPLACE_ME_PUBDATE</pubDate>
            <enclosure
                url="https://github.com/Xpycode/syncthingStatus/releases/download/v1.6.1/syncthingStatus-v1.6.1.dmg"
                sparkle:version="163"
                sparkle:shortVersionString="1.6.1"
                length="REPLACE_ME_LENGTH"
                type="application/octet-stream"
                sparkle:edSignature="REPLACE_ME_SIGNATURE"
            />
            <sparkle:minimumSystemVersion>15.5</sparkle:minimumSystemVersion>
        </item>
