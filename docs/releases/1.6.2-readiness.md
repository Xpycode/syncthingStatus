# Release readiness — 1.6.2 (165)

2026-09-17: user approved disabling cleanup, verification and publication. The inherited release blocker is resolved for this patch by disabling the feature rather than importing unfinished v1.7 work.

| Bucket | Finding | Disposition |
|---|---|---|
| 1 — resolved | Cleanup previously could delete against an ancestor/obsolete bookmark root. | Resolve action removed, legacy window-opening entry refuses, deletion entry returns a visible error and no success, filesystem deletion implementation removed. Four selected-input refusal checks pass; sentinel unchanged. |
| 2 — validation limit | Homebrew full audit requires Xcode 27.0; this Mac has 26.6. | Audit attempted; environment blocked. Cask style passed. Public fetch follows publication. |
| 3 — existing polish | Broader v1.7 Settings, accessibility and app-citizenship work. | Deferred to v1.7. |

## Final checks

- 20 native folder-row cases pass. User accepted the live layout in build 164; final build 165 preserves that layout and disables cleanup.
- Four refusal cases execute the extracted production deletion entry, preserve a sentinel and produce no success outcome. Source checks confirm no cleanup button, no window-opening route and no deletion implementation. Native production alert rendered and inspected: reports cleanup unavailable.
- Final universal Release archive/export passed, arm64 + x86_64, minimum macOS 15.5. Developer ID/hardened runtime/sandbox plus nested Sparkle signing verified.
- Final app and DMG separately notarized and stapled; Gatekeeper accepts both. Strict absolute-path signature check passed. Sparkle signature verified independently against the app's embedded public key.
- Cask style, JSON/XML consistency and website shared-file invariants passed. Local browser checks show release notes/version/date and intact download/Homebrew links. No horizontal overflow at 819/392 CSS pixels; 312 CSS pixels remains a narrow-page limitation.
- User explicitly requested installed 1.6.1 remain running for the update test. The final 165 release is not launched over it. Actual 1.6.1→1.6.2 installation acceptance remains with the user after publication.
- README, CHANGELOG, release notes and website all disclose disabled cleanup. Monitoring and stuck-deletion detection remain available.

## Final artifact

- Version 1.6.2, build 165; 3542329 bytes.
- SHA-256 `51641e72c54458147b8ca3c8133b017db3d3b6ad6b24a7366fdbcbe71caf09fe`.
- Release DMG: `04_Exports/1.6.2-release/syncthingStatus-v1.6.2.dmg` in the primary checkout.
- Supersedes the unpublished build 164 artifact and signature. Use only the build 165 manifest.

## Publication order

Publish GitHub source/tag/DMG first and verify the public asset hash; then promote the signed appcast and cask to main; deploy the scoped homepage payload and verify hashes/version metadata. Preserve counters/feedback and the running installed 1.6.1. Record live results in the session log.

## Publication completed

GitHub release/tag, live main appcast/cask/README/changelog, and homepage are published. Fresh public Homebrew fetch passed. GitHub and homepage downloads match the final SHA-256; feed build/signature/length and catalogue version match. Counters and feedback preserved. Installed 1.6.1 remains running for the user’s update-install test. See the September 17 session log for results and limits.
