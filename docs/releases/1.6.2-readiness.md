# Release readiness — 1.6.2 (164)

Prepared 2026-09-17; not published. The user accepted the repaired dropdown in a screenshot of Debug build 164. A separate Release archive was built from the same final app source.

| Bucket | Finding | Disposition |
|---|---|---|
| 1 — existing release blocker | The v1.6.1 cleanup action can resolve deletion relative to an ancestor or obsolete bookmark root. Confirmed in the September 5 review. This layout-only hotfix inherits that behavior. | Publication paused for the user's scope/risk decision; the separately validated v1.7 cleanup changes are not included. |
| 2 — validation limitation | Full Homebrew audit requires Xcode 27.0; this Mac has 26.6. The audit was attempted again and stopped at that prerequisite. | Cask style passed; public fetch and installation/upgrade cannot be claimed before publication. Preserve the installed 1.6.1 for the user's update test. |
| 3 — existing polish | Broader accessibility, Settings, documentation decomposition and app-citizenship improvements remain in the v1.7 backlog. | Outside this focused patch. |

## Completed checks

- 20 native compact-row cases passed, including long/short names, two widths and five status cases; before/after native screenshots inspected.
- User accepted the live repaired dropdown in 1.6.2 (164).
- Final source changes are limited to compact-row layout and version/build relative to the release baseline. Current main's documentation/Homebrew history was merged without adding v1.7 source.
- Universal Release archive/export passed: arm64 and x86_64, macOS 15.5 minimum, Developer ID, hardened runtime, sandbox/network entitlements, signed nested Sparkle components.
- App and DMG separately accepted by Apple, stapled, and accepted by Gatekeeper. Read-only mounted DMG app passed strict signature and stapled-ticket checks.
- Sparkle signature independently verified using CryptoKit against the release app's embedded public key; signature/byte length/version match the staged feed and manifest.
- Signed release app launched and its exact executable path verified; then explicitly restored `/Applications/syncthingStatus.app` version 1.6.1 for the user's update test.
- Homebrew cask style passed. Website shared-file invariants passed; parsed catalogue differs only in syncthingStatus's version/date. Local browser preview shows 1.6.2 and new notes; desktop (819 CSS pixels) and phone-sized (392 CSS pixels) previews had no horizontal overflow. The requested 390-pixel browser viewport reported 312 CSS pixels (browser scaling) and 363 pixels of content width; that narrow-width limitation is recorded rather than marked passed.
- README and CHANGELOG updated; release notes, cask, complete signed appcast draft and website payload prepared. Root appcast remains unchanged until the DMG is public.

## Artifact

- DMG: `04_Exports/1.6.2-release/syncthingStatus-v1.6.2.dmg` in the primary checkout.
- SHA-256: `cd1e15a706605726edf613e9411829e8296d5cf2d0430c6e4362465dc6320f47`
- Bytes: 3657481.
- App notary and DMG notary logs retained locally; DMG submission: `1f6f4e8b-9bd6-40d9-a33b-acdf2a2a7f5e`.

## Publication order after the release decision

1. Commit/push source and release notes on the hotfix branch; tag that source and publish its GitHub DMG/release notes.
2. Download public DMG and verify exact SHA-256. Only then promote `docs/releases/1.6.2-appcast.xml` to `appcast.xml` and publish main with the cask metadata.
3. Deploy only the staged website payload (page, catalogue, sitemap, DMG), preserving counters and feedback. Merge the website source branch and verify live contents/checksums.
4. Fetch/verify the public Homebrew cask. Keep the user's installed 1.6.1 open for an actual Sparkle installation test; do not upgrade it during automated checks.
5. Record live URLs, results and any limits. If publication happens on a later day, align the catalogue, sitemap, changelog and appcast dates before publication.
