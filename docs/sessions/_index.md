# Session Index

- [2026-07-12](2026-07-12.md) — **Appcast incident hotfixed + 🔴 sandbox/tilde release blocker FIXED & verified.** The drafted 1.6.0 appcast item (REPLACE_ME placeholders) had gone live — every 1.5.5 install offered a 404ing update; item parked in `docs/appcast-v1.6.0-draft.xml` (`88305d9`). Then the blocker: real-home `~` expansion via `getpwuid` (`realPath`/`realURL`) at probe, grantAccess, panel, reveals; probe rewritten on `stat(2)`+errno (research agent *empirically refuted* "sandbox blocks stat" — metadata reads are allowed; ENOENT vs EPERM distinguishable); `fileResourceIdentifier` equality for the panel-pick compare. DEMO_OOS eyeball (fresh injections, reverted after): grant accepted, bookmark saved at real path, delete loop 2 ok/0 failed, rescan fired, bookmark survived relaunch. UX fix from eyeball: window now probes at open (gate-first, user pick). **Release UNPAUSED** — remaining: Rescan ↻ eyeball, `/check ship`, notarized cut. — **Reconciled git, then closed the OSLog release gate.** Session 1: ran `git-bootstrap` (init → fetch → `reset --mixed` → verify), committed the v1.6.0 catch-up (`6318080`) and pushed — repo canonical again. Session 2: full-codebase review found the print()→OSLog conversion already done bar one DEBUG line (removed); fixed false-Disconnected flash on cancelled refresh + locale-fragile "cancelled" string-matching (5 sites → typed `isCancellationError`); removed dead update-check setting. Build green (unsigned verify — no dev cert on this Mac). **No blockers left**; release work remaining: live eyeball on M1 Max, README + release notes, notarized build. Session 3: drafted v1.6.0 release notes in both README + appcast (placeholders for the signed DMG, `c92b947`); first live run on the M1 Max — found *neither* Mac has a dev cert (built Debug **ad-hoc** to run); fixed a popover folder-name line-wrap (`layoutPriority` + `lineLimit`, user-confirmed); Rescan/cleanup eyeball still blocked (daemon all-idle). Noted: fresh build (SDK 26.5) now appears in the macOS 27 beta menu bar — unconfirmed cause, not cookbooked. **Session 4: attempted the release — DEMO_OOS eyeball confirmed the cleanup UI + ⌘A work and the destructive action fails closed, but caught a 🔴 release blocker: the stuck-deletes access flow can't complete for `~/`-configured folders under sandbox (`fileExists` doesn't expand `~` → misleading "path may differ" error; suspected companion bug in grantAccess's container-path compare). Release PAUSED; next session confirms the grant bug then fixes. Notarization sorted (reuse `conjoyn-notary`, creds are account-wide).** — **Session 5 (evening): 🚀 v1.6.0 RELEASED.** Rescan ↻ eyeball ✓ (DEMO_OOS rig, reverted); `/check ship` 0 blockers — its two should-fixes shipped in-release (Launch at Login had **never** worked: `SMAppService.mainApp` fix; pause/resume no longer silent). Release pipeline scripted (`tools/notarize.sh` + `make-dmg.sh`, hdiutil DMG, both notary passes Accepted); GitHub release + appcast + website all live-verified, Check-for-Updates passed on the installed build. Post-release find: About "not connected" while connected — `fetchVersion` nilled the version on every cancelled refresh (refresh group overruns 10-s slot with offline devices); guarded on main (`63eb3fd`), deliberately not re-cut.
- [2026-06-03](2026-06-03.md) — Diagnosed a real ProPro stuck-delete (shared `.git` over Syncthing + unmerged `feature/aloft-icons` branch flip); recovered icons, fixed infra (`.git`/`.stversions` ignored on both Macs, M1max→staggered versioning). **Added inline Rescan button to the out-of-sync folder row** (verified via temp DEBUG injection, reverted). Clean build green. **⚠️ Discovered this project's local `.git` is GONE on both Macs; GitHub remote stale since 2026-04-28 — repo recovery deferred to next session (plan in log).**
- [2026-05-01](review-2026-05-01.md) — Production review for v1.6.0 (build 162). Bookmark lifecycle, path validator, destructive path, diagnostic export all pass. **High: 30+ bare `print()` calls in production paths defeat the new diagnostic export** — convert to OSLog before Release cut.
- [2026-04-29](2026-04-29.md) — v1.6.0 stuck-deletes feature end-to-end (Phases 0–4 + bookmark refactor for sandbox); evening pass added `DiagnosticLogger` export, About-panel Syncthing version, popover dual-version display, and ⌘A select-all in the cleanup window.
- [2026-04-28](2026-04-28.md) — Fix false-red status icon, add traffic-light mode, integrate new app + status icon assets, add notification cooldown
