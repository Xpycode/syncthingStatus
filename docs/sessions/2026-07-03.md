# Session: 2026-07-03

## Goal
Reconcile git — re-establish the local repository that Syncthing had stripped, and get all
uncommitted v1.6.0 work back under version control on GitHub.

## Progress
- **Diagnosed via `/status`:** repo blocker from 2026-06-03 was still open — no `.git` on disk,
  origin `Xpycode/syncthingStatus` stale at `a28caa8` (2026-04-28, v1.5.5 era). All v1.6.0 work +
  docs existed only in the working tree.
- **Ran the `git-bootstrap` skill** (safe, non-destructive to files):
  - Confirmed constants first: global identity already set (`Luces Umbrarum`), `gh` authed over
    HTTPS, remote exists with default branch `main`.
  - `git init -b main` → `remote add origin` → `fetch` → **`reset --mixed origin/main`** (the safety
    pivot: repoints HEAD/index at origin's stale tip while leaving every file on disk untouched).
  - Read THE CHECK: 13 tracked modified + 11 untracked, all forward v1.6.0 work. Verified it was
    **not a divergence** — origin's last push (04-28) predated every local session log, so origin
    held nothing the disk lacked. Confirmed the 61 source deletions were tiny refactors inside
    massively-added files (`Client.swift` +611, `Views.swift` +522), not lost origin content.
  - `.gitignore` verified to cover `.claude/settings.local.json`, `.serena`, `.fastembed_cache`,
    `.DS_Store` — no per-machine noise leaked into the commit.
- **Catch-up commit `6318080`** (24 files, ~+1500 lines) → pushed fast-forward `a28caa8..6318080`.
- **Synced `PROJECT_STATE.md`:** moved the git blocker to a new **Resolved** section, dropped the
  now-done "re-establish repo" next action + renumbered, bumped last-updated. Committed + pushed as
  `75e4ef1`.
- Final state: local `main == origin/main`, tree clean, origin canonical again.

## Decisions
- **Committed all v1.6.0 work as a single catch-up commit** rather than trying to reconstruct
  granular history — the intermediate commits were lost with `.git`, and the working tree is the
  only source of truth. Origin was strictly behind, so this is a clean fast-forward, not a merge.
- **Used `reset --mixed`, never `--hard`** — the mixed reset is what surfaces on-disk drift *before*
  anything is overwritten; `--hard` would have silently destroyed all v1.6.0 work.

## Next
- **OSLog conversion** is now the sole remaining release gate: replace the 30+ bare `print()` calls
  in production paths with categorized OSLog calls (2026-05-01 review, High).
- Then: eyeball the inline Rescan button live, live-verify cleanup ⌘A, draft v1.6.0 release notes +
  README, cut the notarized Release build. Run `/check ship` before release.
- Root cause is mitigated (`.git`/`.stversions` ignored on both Macs) but stays worth watching —
  if `.git` vanishes again, re-run the `git-bootstrap` skill.

---

# Session 2 (same day): code-quality pass — OSLog gate closed, cancellation bugs fixed

## Goal
Full-codebase review (all 12 Swift files, ~6.9k lines): refine, remove bugs, close open issues.

## Progress
- **OSLog release gate closed.** Discovery: the "30+ bare `print()`" blocker was already ~done —
  the v1.6.0 catch-up work had converted them. Only one remained (`Client.swift` `makeRequest`,
  DEBUG-only, printed the API-key *length* on every request). Removed. `grep print(` is now clean.
- **Bug: `fetchStatus()` treated cancellation as disconnection.** Every other fetcher skipped
  cancelled requests; this one set `isConnected = false` + error banner when `refresh()` cancelled
  a superseded in-flight task → false "Disconnected"/red-icon flash. Now guards first.
- **Bug: cancellation detected via `localizedDescription.contains("cancelled")`** in five places —
  locale-dependent (on a Danish system the description isn't "cancelled", so the guards silently
  failed and transient errors leaked to the UI). Replaced with a typed helper
  `isCancellationError(_:)` (`CancellationError` / `URLError.cancelled`).
- **Cleanup:** `AppConstants.Polling.*DelayMs` renamed → `*Ns` (values were always nanoseconds —
  fed to `Task.sleep(nanoseconds:)`; the Ms name was a latent unit trap). Removed dead
  `SyncthingSettings.automaticallyCheckForUpdates` (Settings UI binds Sparkle's own
  `automaticallyChecksForUpdates` directly — the stored setting drove nothing) and unused
  `hasSignificantActivity()` helper.
- **Build:** this Mac lacks the "Mac Development" cert (team FDMSRXXN73), so verified with
  `CODE_SIGNING_ALLOWED=NO` — **BUILD SUCCEEDED**, zero errors/warnings. Runtime eyeball still
  pending on the M1 Max (see PROJECT_STATE Next Actions).
- PROJECT_STATE updated: blocker section now empty; OSLog gate moved to Resolved.

## Decisions
- **Cancellation checks are typed, never string-matched** — `error is CancellationError ||
  (error as? URLError)?.code == .cancelled`. `localizedDescription` is localized by definition;
  matching English substrings breaks on non-English systems.

## Next
- Remaining release work: live-eyeball Rescan button + cancellation fixes on M1 Max, cleanup-window
  ⌘A live test, v1.6.0 release notes + README, notarized Release build.
- **Ideas parked (post-release candidates):** switch polling → Syncthing event API (`/rest/events`
  long-poll) for instant updates; unit tests for `StatusIconStateResolver.resolveState` +
  `validatePath` (both pure, no daemon needed); demo mode should fake `isConnected` so screenshots
  work without a live daemon; `Localizable.xcstrings` pass while string count is small; move the
  three root-level DMGs + screenshot scripts out of the synced repo root.

---

# Session 3 (same day): v1.6.0 release notes drafted + first live run on M1 Max + popover fix

## Goal
Release-prep item 3 (release notes + README), then — now sitting at the M1 Max — do the deferred
live run, and fix a user-reported popover layout bug.

## Progress
- **v1.6.0 release notes drafted in both channels** (README + Sparkle appcast, kept in lockstep):
  - `README.md`: new `## What's New in Version 1.6.0` (stuck-deletion cleanup, sandbox-safe folder
    access, inline Rescan, diagnostic export, dual-version display); version/Download badges
    `1.5.5→1.6.0`; v1.5-users manual-download note re-pointed; Features + API-endpoint lists refreshed
    (`db/need`, `db/scan`).
  - `appcast.xml`: 1.6.0 `<item>` mirroring the notes, with `edSignature`/`length`/`pubDate` left as
    loud `REPLACE_ME_*` placeholders (only fillable after the signed DMG) + a release-cut checklist
    comment. `sparkle:version=162` (verified > `155`, so Sparkle's monotonic rule holds).
  - Committed + pushed `c92b947`.
- **First live run on the M1 Max.** Clean build cycle. Discovery: **neither Mac has a dev-signing
  cert** — this M1 Max has only `Developer ID Application` (the release/notarization cert, team
  FDMSRXXN73), no "Mac Development"/"Apple Development". Debug automatic signing fails on it.
  Built Debug **ad-hoc** (`CODE_SIGN_IDENTITY="-"`, command-line only, no file changes) → runnable,
  sandbox entitlements still applied. App launches (pid confirmed), connects to the live daemon.
- **Rescan button + cleanup ⌘A still not eyeballed** — queried `db/status` for all 4 folders: every
  one `idle`/in-sync (`needTotal=0`, `needDeletes=0`), so the out-of-sync row (the button's only
  home) doesn't render, and there's no stuck-delete repro. Both remain pending a real out-of-sync
  or the `DEMO_OOS` injection trick.
- **Fixed popover folder-name line-wrap (user-reported).** "ClaudeSessions" wrapped to two lines in
  `FolderStatusRow.compactView` (`Views.swift:1314`). Root cause: the two trailing numeric columns
  are each `maxWidth: .infinity` and squeezed the leading name/path column, which — unlike the path
  line — had no `lineLimit`. Fix: `lineLimit(1)` on the name + `.layoutPriority(1)` on the name/path
  `VStack` so it claims needed width first (chosen over ellipsis-truncation via `AskUserQuestion` —
  full name preserved). Rebuilt ad-hoc; user confirmed it now sits on one line.
- **Observation (not yet a pattern):** this fresh build — linked against **MacOSX26.5.sdk** — now
  appears in the **macOS 27 beta** menu bar, whereas the shipped 1.5.5 (older SDK) did not. Menu bar
  is a manual `NSStatusItem` + `.accessory` policy; *no code changed it this session.* Hypothesis:
  building against the current SDK restores `NSStatusItem` visibility under Tahoe's menu-bar rework.
  **Unconfirmed → deliberately NOT written to the cookbook yet** (cookbook needs cause + repro).

## Decisions
- **Ad-hoc "Sign to Run Locally" for Debug on cert-less Macs.** With no development cert on either
  Mac, `CODE_SIGN_IDENTITY="-"` is the non-invasive way to get a runnable, sandboxed Debug build for
  eyeballing. The Release deliverable is unaffected (Developer ID → notarytool still works). Proper
  long-term fix: add an *Apple Development* cert via Xcode → Settings → Accounts (a user action).
- **Popover name overflow resolved by layout priority, not truncation** — keep the full folder name
  on one line; only a pathologically long name would truncate.

## Next
- **Menu-bar-on-27-beta:** re-verify when the real Release build is cut; promote to a cookbook entry
  *only if* the SDK-rebuild causation holds up.
- **Still pending eyeball:** Rescan button (needs out-of-sync or `DEMO_OOS`), cleanup ⌘A (needs a
  stuck-delete repro).
- **Release cut:** Developer ID build → `notarytool` → DMG → Sparkle EdDSA sign → fill the appcast
  `REPLACE_ME_*` placeholders → GitHub release. Run `/check ship` first.
- **Optional infra:** add an Apple Development cert to this Mac so Debug builds sign normally (drop
  the ad-hoc workaround).

---

# Session 4 (same day): release attempt → DEMO_OOS eyeball caught a release-blocking sandbox/tilde bug

## Goal
Release v1.6.0 to GitHub + the app website. Along the way, run the DEMO_OOS eyeball (Rescan button
+ cleanup ⌘A) the user asked for before shipping.

## Progress
- **Website terrain mapped.** syncthingStatus is *already* catalogued on the showcase site
  (`3-Websites/App-Websites/APPS/apps.lucesumbrarum.com`): entry in `apps-data.md` (line 102),
  showcase page `public/apps/syncthingstatus.html`, download via `dl.php?app=syncthingstatus` which
  **302s to a self-hosted `/downloads/syncthingstatus.dmg`** (NOT a GitHub passthrough — the site
  needs its own DMG copy, deployed via lftp). So website work = *update* version/features/size +
  redeploy, plus copy the new DMG into the site's downloads.
- **Notarization confusion resolved.** notarization credentials are **not per-app** — one Apple ID +
  app-specific password notarizes every app under team FDMSRXXN73; per-app profile names are just
  labels. This Mac already has a working profile: **`conjoyn-notary`** (`notarytool history` succeeds).
  Decision: **reuse `conjoyn-notary`** for syncthingStatus — no new password, nothing to create.
  syncthingStatus has *no* release scripts; Conjoyn/Magpie's `make-dmg.sh`/`notarize.sh` are the
  proven template to adapt.
- **DEMO_OOS eyeball (two injections, `#if DEBUG` + `DEMO_OOS=1` gated, since REVERTED).** Forced
  the first folder to idle+needDeletes and injected fake cleanup candidates. **Confirmed working:**
  popover orange stuck-delete alert row + "Resolve…"; out-of-sync "2 deletes" state; cleanup window
  opens; **Select All / Deselect All / ⌘A all work** (the previously-unverified bit); delete
  confirmation dialog; destructive action **fails closed** (nothing deleted without access). The
  inline **Rescan ↻ button was NOT seen** — it lives in the *detailed* row (`folderStatusLabel`,
  main Window), not the popover compact row; still un-eyeballed.
- **🔴 RELEASE-BLOCKER FOUND — stuck-deletes access flow is broken for `~/` folders under sandbox.**
  - **Confirmed:** all Syncthing folder paths are literal `~/…` (`~/SYNCsim`, etc.).
    `probeFolderAccess` (Client.swift:1909) does `fileExists(atPath: "~/SYNCsim")` — `fileExists`
    never expands `~`, so it's always false → returns `.notFound` → the misleading *"Folder root not
    found / the path may differ between peers"* error, which blocks the grant-access prompt. Hits
    **every folder the user has.** Not a DEMO artifact (the probe ran on the real path/bookmark state).
  - **Suspected (unverified — next session's job):** the app **is** sandboxed (confirmed: built
    binary has `app-sandbox` + `files.user-selected.read-write`), so `~` expands to the *container*
    home, not `/Users/sim`. `grantAccess` (Client.swift:1847) compares the user's real NSOpenPanel
    pick against `standardizingPath("~/…")` (container path) → they can't match → it would **reject
    the valid folder selection**. So fixing the probe alone likely isn't enough.
  - **Root cause:** the app resolves Syncthing's `~/…` paths with container-aware APIs
    (`expandingTildeInPath`/`standardizingPath`) or not at all, when it needs the **real** home.
  - **Why it survived:** PROJECT_STATE's "verified 2026-06-03" was git-resolved, so the app's own
    grant→delete path never ran against a real `~/` folder under sandbox.
- **Corrected a self-inflicted false alarm:** first `codesign -d --entitlements -` used wrong syntax
  and showed "no entitlements"; the correct `--entitlements :-` shows the app is properly sandboxed
  with file-access entitlements. Entitlements are fine.
- **Release PAUSED.** User chose: **confirm the grant bug first**, then fix, in a NEW session.

## Decisions
- **Reuse the `conjoyn-notary` keychain profile** for syncthingStatus notarization — credentials are
  account-wide, not per-app; the profile name is cosmetic. No new app-specific password.
- **Pause the v1.6.0 release** — the marquee stuck-deletes feature can't complete its core action for
  `~/`-configured folders (the common case). Fix + real end-to-end verification required before ship.

## Next
- Release remains paused pending the sandbox/tilde fix (below). Everything else (notes, website copy)
  is ready to go once the feature works.

## Resume  (START HERE next session — confirm the grant bug, then fix)
**Task:** empirically confirm the *suspected* `grantAccess` container-mismatch before writing the fix.
1. **Confirm how `~` resolves under this sandboxed app.** Add a temporary diagnostic (DEBUG, log via
   `stuckDeletesLog`) that prints, from inside the running app:
   `("~/SYNCsim" as NSString).expandingTildeInPath`, `.standardizingPath`,
   `String(cString: getpwuid(getuid()).pointee.pw_dir)`, and
   `FileManager.default.homeDirectoryForCurrentUser.path`. Build ad-hoc (`CODE_SIGN_IDENTITY="-"`,
   see memory [[dev-signing-cert-gap]]), run, read via diagnostic export / Console. Compare container
   vs real home. Also optionally drive a real grant (NSOpenPanel → pick `/Users/sim/SYNCsim`) and see
   whether `grantAccess` accepts or rejects.
2. **Then design the fix** (see memory [[sandbox-tilde-real-home]]): a real-home `~` expander using
   `getpwuid(getuid())→pw_dir`; apply at `probeFolderAccess` (Client.swift ~1907) **and** `grantAccess`
   (~1843); make the probe default to `.needsBookmark` when it can't stat under sandbox (don't
   short-circuit to `.notFound`). Re-verify the **full** grant→delete flow via DEMO_OOS on a rebuilt
   sandboxed build — not just a compile.
**State:** working tree clean; DEMO_OOS injection reverted; popover fix + all docs committed & pushed.
**Files:** `Client.swift` `probeFolderAccess` (~1907), `grantAccess` (~1843), `deleteOne` (~2020).
**Release when done:** reuse `conjoyn-notary`; adapt Conjoyn/Magpie `make-dmg.sh`+`notarize.sh`;
fill appcast `REPLACE_ME_*`; then GitHub release + website (`apps-data.md` + `syncthingstatus.html` +
copy DMG to the site's `downloads/` + `deploy.sh`).
