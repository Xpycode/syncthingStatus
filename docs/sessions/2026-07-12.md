# Session: 2026-07-12

## Goal
User reported an update error on installed 1.5.5 → turned into two fixes: the live-appcast incident,
then the release-blocking sandbox/tilde bug (the "confirm + fix" job queued from 2026-07-03 Session 4).

## Progress
- **🔥 Appcast incident (self-inflicted, hotfixed).** The drafted 1.6.0 `<item>` with `REPLACE_ME_*`
  placeholders went live in `c92b947` — `appcast.xml` on `main` **is** the production Sparkle feed
  (SUFeedURL → raw.githubusercontent `main`). Every 1.5.5 install was offered a 1.6.0 whose DMG 404s
  ("An error occurred while downloading the update"). Hotfix `88305d9`: item parked in
  `docs/appcast-v1.6.0-draft.xml`, warning comment left in the feed, verified live feed tops out at
  1.5.5 again. **Lesson: the appcast on `main` is production infrastructure — drafts must live where
  Sparkle can't see them.** (Also: don't click "Skip This Version" on the phantom prompt — the real
  1.6.0 ships as the same build 162 and would be suppressed.)
- **🔴→✅ Sandbox/tilde blocker fixed** (`Client.swift`, `Helpers.swift`, `App.swift`, `Views.swift`):
  - `realHomeDirectoryPath()` (`getpwuid`) + `expandingTildeToRealHome(_:)` +
    `SyncthingFolder.realPath/.realURL`; applied at the probe, `grantAccess`, the grant panel's
    `directoryURL`, both Finder reveals, and `defaultSyncthingConfigDirectory()` (same disease).
  - Probe rewritten on `stat(2)`+errno: ENOENT → honest `.notFound`; EPERM → `.needsBookmark`.
    Post-grant `contentsOfDirectory` NoSuchFile now also maps to `.notFound`.
  - `grantAccess` canonicalizes both URLs and accepts `fileResourceIdentifier` equality (APFS
    case-insensitivity, firmlinks).
- **Research agent earned its keep:** it *built and ran a sandboxed test binary* on this Mac —
  confirmed getpwuid, confirmed ALL Foundation home APIs (incl. `NSHomeDirectoryForUser` and `HOME`)
  are container-redirected, and **refuted** my "can't stat outside the sandbox" assumption:
  `application.sb` has a blanket `(allow file-read-metadata)`; only *content* access is denied, and
  denial is EPERM vs ENOENT — distinguishable. That refutation upgraded the fix (see decisions.md).
- **DEMO_OOS eyeball (injections re-created fresh, since REVERTED again):** full grant→delete verified
  on the sandboxed Debug build — OSLog trace shows probe "needs grant" (not the old `.notFound`),
  panel pick **accepted**, bookmark saved at `/Users/sim/SYNCsim` (real home!), scoped delete loop
  2 ok / 0 failed, rescan triggered. Nothing real deleted (fake names; ENOENT-as-success by design).
  Bookmark **persisted across app relaunch and even a cfprefsd kill** — "grant once per folder" holds.
- **UX finding from the eyeball → gate-first fix** (`Views.swift` window `.task`): access was only
  probed in the delete pre-flight, so the grant panel interrupted mid-delete and the reload dropped
  the selection (select-twice). Now probes at window open; user chose gate-first over lazy+preserve.
- **Unexplained (macOS 27 beta, parked):** the app's `UserDefaults` demonstrably persist, but no
  `FolderAccessBookmark` key exists in any container/Preferences plist findable on disk. Standard
  API usage — likely a Tahoe prefs-storage relocation, not an app bug. Watch on release macOS.
- **Housekeeping:** `log` is a zsh *builtin* — use `/usr/bin/log` for OSLog queries in scripts.

## Next
- Eyeball the inline Rescan ↻ button (detailed row in the main Window — still the only unverified UI).
- `/check ship`, then release cut: Developer ID build → `conjoyn-notary` → staple → DMG → Sparkle
  EdDSA sign → fill the parked draft's placeholders → move item into `appcast.xml` → GitHub release →
  website (`apps-data.md` + `syncthingstatus.html` + DMG copy + `deploy.sh`).
- Promote the sandbox-tilde + stat/errno gotcha to the Directions cookbook / `22_macos-platform.md`
  — now proven (memory `sandbox-tilde-real-home` updated).

---

# Session: 2026-07-12 (evening) — 🚀 v1.6.0 RELEASED

## Goal
Finish the release: last UI eyeball, `/check ship`, then the full notarized cut.

## Progress
- **Inline Rescan ↻ eyeballed** (the last unverified UI) via a fresh DEMO_OOS rig (needFiles-based so
  the stuck-latch stays quiet; temp success-log in `rescanFolder`). Button renders, tooltip correct,
  click POSTed `db/scan` — OSLog-confirmed. Rig reverted, tree clean. Side-finding: `db/scan` blocks
  until the scan completes, so repeat clicks time out (log-only, harmless). Tooltip delay = system
  default; left stock.
- **`/check ship` passed** — ~30 checks, 0 ship-blockers. Its two should-fixes were fixed + live-verified
  + shipped in 1.6.0 (`b3d1977`): **Launch at Login had never worked** (wrong API — needs
  `SMAppService.mainApp`, not `.loginItem(identifier:)` with the stale pre-v1.4 bundle ID; no helper
  bundle exists) and **pause/resume failed silently** (5 catch blocks now feed `lastErrorMessage`,
  cancellation-filtered, demo-excluded). Wrote the "done for v1.6.0" ship line into PROJECT_STATE.
- **Release pipeline scripted** (`a4e7d49`): `tools/notarize.sh` + `tools/make-dmg.sh` adapted from
  Magpie — archive → developer-id **export** (re-signs Sparkle's nested XPC), nested-Mach-O + entitlements
  asserts (sandbox/network.client live in build settings), hdiutil plain DMG (create-dmg Finder styling
  broken on macOS 26+), `conjoyn-notary`, both notary passes **Accepted** first try.
- **Shipped end-to-end:** GitHub release v1.6.0 + DMG (3,649,427 bytes, URL 200-verified **before**
  the appcast push — the morning's incident rule), appcast item live (`222a292`, sig + length + pubDate
  filled, draft file retired), website bumped + deployed + verified (v1.6.0 spans, new stuck-delete
  feature card, dl.php serves the new DMG; site repo `e69aad5`), notarized build installed to
  /Applications, **Check for Updates passed against the live feed**.
- **Release-verify caught a real (cosmetic) bug** (`63eb3fd`, on main only): About said
  "Syncthing: not connected" while demonstrably connected. Root cause: `fetchVersion()`'s catch nilled
  `syncthingVersion` on *cancelled* refreshes — the one site the July cancellation fix missed — and with
  offline devices the refresh group overruns its 10-s slot, so every cycle cancels its predecessor
  (`system/version: cancelled` every 10 s). Guard added (keep last-known value). Deliberately **not**
  re-cut into 1.6.0; also present in 1.5.5. Installed 1.6.0 still shows it — expected.

## Decisions
- **No re-cut for cosmetic post-release finds** — the About flap rides the next release.
- Plain hdiutil DMG is the house default while create-dmg is broken on macOS 26+.
- Local-only hiccup, not a bug: first launch of the Developer-ID build sat "not connected" for minutes —
  this Mac's Keychain/TCC entries were minted by ad-hoc Debug builds; field users (1.5.5 was same
  Developer ID team) are unaffected.

## Next
- **Cookbook promotions:** sandbox-tilde + `stat(2)`/errno probe; `SMAppService.mainApp` vs
  `.loginItem(identifier:)` (silent no-op toggle, survived 3 releases).
- **v1.7:** refresh-overrun root cause (suspect `db/completion` on offline devices holding to the 30-s
  resource timeout); bucket-3 backlog in PROJECT_STATE (citizenship packages, frame autosave,
  CHANGELOG, file splits, About live-refresh).
- Optional user-side: revoke the stale FDA grant for syncthingStatus.app.

## Resume
- **Tree clean, everything pushed** (app repo through `63eb3fd`; site repo `e69aad5`; release + feed live).
- No half-done edits. Pick up at: cookbook promotions (PROJECT_STATE → Next Actions 1) or the v1.7
  refresh-overrun investigation (Next Actions 2).
- Memory `release-notarization-workflow` rewritten — the next cut is `tools/make-dmg.sh` + sign_update
  + appcast fill; push the appcast only after the DMG URL serves 200.
