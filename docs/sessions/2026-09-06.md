# Session: 2026-09-06 — cleanup safety handoff

Continued the authorized cleanup safety sprint and finalized the September 5 implementation/verification records. Tasks 1.2–1.5 are complete; A1–A3 passed. No later implementation wave or release was started.

- Reproduced wrong-root and stale-identity deletion through production controller tests; fixed access scope, configured-root identity and confirmation binding.
- Independent review found grant replacement after confirmation and an access-recovery timing issue; both fixed and re-reviewed without remaining blockers.
- Final suite: 49 tests passed, including 40 cleanup tests. Real sandbox/native UI checks passed with production-equivalent filesystem entitlements; picker, persistence, root changes, permissions, selection, Cancel, retry, partial failure and contained mutation were exercised on disposable fixtures.
- Debug build and strict signature check passed. The old installed app quit gracefully and exactly one fresh process was verified at `/private/tmp/syncthingStatus-v17-build/Build/Products/Debug/syncthingStatus.app` on September 5.
- GitHub #2 monochrome rendering, #5 long-path layout and #6 Homebrew follow-up remain explicitly deferred and linked in the tracker/evidence.

[Complete evidence, replay sources, logs and limits](../reviews/evidence/2026-09-05/cleanup-safety.md). Cleanup source/tests/evidence are committed locally as `80aab96` on `fix/isolated-production-tests`; planning/session documentation was preserved for the requested commit-and-push wrap-up. No live cleanup, daemon configuration mutation or release publication.

Next planned work: Wave 2, truthful sync status, when implementation is requested again. Deep reasoning remains appropriate for validity/state and upcoming concurrency work.

## Session wrap-up

User explicitly requested commit, push and log. Fetched origin and verified that the fix branch is ahead of `origin/main` without remote-only commits. Reviewed the remaining documentation changes, retained the prior planning/task 1.1 history, and prepared the session/state checkpoint on the existing fix branch. The `Handoff-from` commit trailer identifies this Mac. Push/remote equality is verified after the checkpoint; no merge or release is requested.

Archived tasks 1.2–1.5, bringing the archive to five completed tasks. Current Sprint is empty; tracked progress is 5/19 (26%), with 14 deferred backlog tasks and two Inbox observations. GitHub #2/#5/#6 remain individually linked. Synced the lean project digest and session index. The historical July 3 index gap remains separately recorded; no unrelated log backfill.

The fresh runnable app handoff remains intentional under the user's standing fresh-build preference; it is not a stray test runner to replace with the older installed release during logging. No new build or test was needed for documentation-only wrap-up.

## Later session — Wave 3 refresh implementation

### Goal

Start the next planned refresh-reliability wave after truthful status. The user subsequently said **stop** during final runtime observation, then requested **log to clear**. This section supersedes the earlier next-session pointer while preserving the cleanup handoff history above.

### Progress

- Reproduced dropped replacement refresh and late real status after demo entry with deterministic production-client gates; both failed before repair. Added coverage for bursts, all connection identity inputs, cancellation unwind, every monitoring success/failure publication boundary, timer replacement, shutdown, demo transitions, bounded workers and notification history.
- Made `SyncthingClient` the owner of monitoring timers and a coalescing refresh drain. Connection changes invalidate immediately and await old work before preparing current credentials. Manual waiters complete at their requested pass boundary, even when subsequent work is queued.
- Guarded publication of status, caches, histories, errors and notifications by generation and cancellation. Old callbacks cannot install timers after shutdown. Unknown configuration cannot authorize requests against the old folder/device list.
- Added two workers per folder/device list, with at most four expensive requests plus version/connections. Delayed first/middle/last entries no longer block unrelated fast publication; entry failures stay local. Production request deadlines and polling interval were retained.
- Measured local daemon endpoints read-only. Two offline peer completion requests took 0.5353 s and 16.7808 s; a folder-status request took 0.0151 s. The overrun is reproduced; a specific 30 s resource timeout is not. Fresh-app logs separately recorded a completion failure after 10.005624 s and subsequent refresh progress.
- Independent review found demo metrics being partially cleared by settings changes and completion starvation while polling remained continuously busy. Reproduced both before fixing; moved global completion tracking from icon rendering to completed client passes and preserved synthetic demo presentation. Fixed an additional connection-outage history gap. Final independent review reported no remaining blockers.
- The existing Refresh button stays enabled during active work and coalesces another request; added its accessibility label. No new UI placement or framework conversion.

### Validation

- **85 tests ×3 = 255 executions, zero failures.** This includes 15 refresh methods with parameterized endpoint/identity/position scenarios and all prior cleanup/status/isolation tests. [Preserved summaries](../reviews/evidence/2026-09-06/refresh/test-results.txt).
- Fresh ad-hoc Debug build passed; signed App Sandbox entitlement inspected. Gracefully quit the old instance, verified all old instances exited, launched the exact new executable under `/private/tmp/syncthingStatus-v17-build/Build/Products/Debug/syncthingStatus.app`, and verified PID 22902 at launch.
- Native smoke: opened the popover, accepted three enabled Refresh actions, opened the details window, and read About showing app 1.6.1 (163) / Syncthing v2.1.2. One redundant menu-bar activation returned an AX error; direct About activation succeeded. [Smoke record](../reviews/evidence/2026-09-06/refresh/runtime-smoke.txt).
- **Not completed:** 25-sample About/version stability watch. The user stopped it and the helper was interrupted; its output was empty. No stability pass or full Wave 3/A5 closure is claimed.
- No release metadata changed, daemon configuration mutation, live cleanup deletion, push or merge occurred. The separate earlier cleanup-session commit/push authorization above does not describe a new commit of this Wave 3 work.

### Decisions

Client-owned pass completion controls global notifications, while the app renders status and starts/stops monitoring. Refill two workers per entry kind under existing deadlines. Keep the confirmed offline overrun distinct from the unconfirmed 30 s timeout. [Standalone decision](../decisions.md#2026-09-06--one-refresh-owner-and-bounded-entry-workers).

### Pre-clear wrap-up

Updated the plan, evidence, task tracker, lean project digest and today's session index. Tasks 3.1–3.3 are checked; 3.4 remains open for the interrupted runtime gate. Archived the completed Wave 2 tracker task, making six archived tasks and 6/19 (32%) progress. There is one active unchecked task, 12 deferred backlog items and two Inbox observations. GitHub #2/#5/#6 remain deferred.

Pre-clear mode does not itself request commit/push. Wave 3 changes and this documentation remain uncommitted on `fix/isolated-production-tests`; its remote-tracking comparison reports one pre-existing local commit ahead. No new fetch or remote reconciliation occurred during logging. The intentional fresh user-testing app was retained under standing user preference, as in the earlier handoff; no runtime work was resumed.

## Resume

- **Exact next action:** when the user requests execution again, finish the bounded About/version stability observation in the fresh app over multiple refresh intervals. The interrupted helper was `/private/tmp/syncthing-refresh-app-control watch-about`; its code/output are temporary and may not survive a restart. Reconstruct a small read-only native check if necessary. Do not repeat the whole implementation or claim the interrupted check already passed.
- **Then:** if the observation passes, close task 3.4/A5, update evidence/state and review the working diff for a separately authorized commit. Wave 4 (notification selection, targeted pause, cleanup pagination and connection recovery) has not started; do not silently promote it during log preparation.
- **Uncommitted source:** `01_Project/syncthingStatus/{App.swift,Client.swift,Constants.swift,Views.swift}`, `01_Project/syncthingStatusTests/SyncStatusTests.swift`, and new `RefreshTests.swift`. No half-written source edit is known. Uncommitted docs include the refresh evidence directory, plan, state, tasks/archive, decisions and session/index changes from this log.
- **Evidence:** [refresh review record](../reviews/evidence/2026-09-06/refresh.md), preserved failing/final test summaries, anonymous offline timings, fresh-app request timing lines and native smoke output. Final local test result: `/private/tmp/syncthingStatus-v17-tests/Logs/Test/Test-syncthingStatusTests-2026.09.06_02-01-16-+0200.xcresult`. Build log: `/private/tmp/refresh-build.log`.
- **App:** fresh sandboxed Debug artifact at `/private/tmp/syncthingStatus-v17-build/Build/Products/Debug/syncthingStatus.app`, exact path verified when launched. Public version remains 1.6.1 (163); this session did not prepare or approve a release. Native windows opened by the smoke may remain visible.
- **Deferred:** 12 backlog tasks (including GitHub #2/#5/#6) and two Inbox observations: the specific 30 s offline-peer resource timeout and disconnected-popover sizing. Offline overrun itself is measured. Homebrew supported-toolchain/full audit and isolated install/upgrade checks remain deferred.
- **Model fit:** implementation capability + medium reasoning for the bounded runtime/documentation finish; use deep capability + high reasoning if a new concurrency failure emerges. Current model setting is not reliably exposed.
- **Historical index:** July 3 is a previously recorded missing entry, unrelated to this wave; no backfill was requested.
