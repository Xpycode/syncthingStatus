# Session: 2026-09-05

## Goal

Summarize project status, migrate the state to a lean digest, add Homebrew
distribution, and record how future releases reach all distribution channels.
Later: review the app/code for usability and unnecessary complexity, then prepare
a committed pre-clear handoff with all fixes deferred to the next session.
Latest follow-up: expand the review-fix implementation plan with Sol/Luna assistance,
then log the completed planning work for a clear; implementation remains next-session work.

## Progress

- Confirmed v1.6.1 is the latest GitHub release and the v1.7 focus remains refresh
  overruns with offline devices.
- Verified every apparently modified/untracked file matched upstream content from
  the other Mac, then aligned local Git history while preserving working files.
- Migrated `PROJECT_STATE.md` to the lean format. Preserved the exact prior state in
  `archive/project-state-2026-08-10.md` and appended ten missing decision summaries
  to `decisions.md`. Checked preservation, digest limits, and local links.
- Published `Casks/syncthingstatus.rb` and installation/maintenance documentation.
  The existing app repository is the custom tap; no separate tap repository or
  app build is needed. Migration and Homebrew work were committed and pushed to
  `main` (`35e3ed9`, `3334a85`, `51cfe63`).
- Homebrew metadata loading and style checks passed. A fresh tap from the public
  GitHub URL found v1.6.1 and fetched the checksum-verified release DMG.
- Mounted the downloaded DMG read-only: app path, bundle ID, version 1.6.1/build 163,
  macOS 15.5 minimum, Intel/Apple Silicon architectures, strict code-signature
  verification, and stapled notarization-ticket validation passed.
- Full Homebrew audit stopped at environment setup because this Mac has Xcode 26.6
  and Homebrew requires 27.0. Installation/upgrade was not tested against the existing
  app. Both remaining checks are now tracked in `TASKS.md`.
- Removed temporary validation taps and restored Homebrew developer mode to off.
  No app build was launched during the distribution work; the later review built
  and launched a fresh Debug artifact as recorded below.
- Recorded the user's requested release-channel checklist in
  `homebrew.md#release-maintenance` and linked it from the state digest.
- Session-index drift check found one pre-existing missing entry for
  `2026-07-03.md`; today's entry is present. The older gap was surfaced and left
  unchanged rather than backfilling unrelated history during this plain log request.
- Follow-up at the user's request: added a Homebrew badge linking to README install
  instructions, Homebrew/cask links, and upgrade commands; publishing these with
  the release checklist and session documentation to GitHub.
- Left `APPS/apps.lucesumbrarum.com/docs/HANDOFF-syncthingstatus-homebrew.md` in the
  App-Websites project and linked it from its root `docs/TASKS.md`. It includes exact
  installation commands, copy, links, future-release maintenance, and deployment
  checks. These two website documentation edits remain local; existing website
  changes were preserved, and no site content was edited or deployed.

- Completed a full current-source review with three smaller agents (code, usability,
  web), verified their findings, and saved the ranked report in
  `reviews/2026-09-05-usability-code-review.md`.
- Confirmed a release-blocking cleanup target-root error: ancestor grants and old
  bookmarks can target data outside the currently configured Syncthing folder.
  Isolated temporary fixtures reproduced sibling/old-root deletion; no user data
  was involved. Do not use cleanup until corrected.
- Extracted-source/stubbed-request harnesses reproduced false In sync results and
  cancellation discarding a replacement refresh. Compared notification predicates:
  empty selection renders all folder switches off but enables all notifications.
  The specific offline-peer timeout cause remains unmeasured.
- Built successfully with Xcode 26.6 (ad-hoc Debug), gracefully quit the installed
  instance, and launched the exact fresh artifact. Inspected live popover, Settings,
  main window, and accessibility output with offline peers and idle folders.
  Build log and screenshots are in `/private/tmp/syncthingStatus-review-*` on M1-Max.
- Preserved the reproduction sources and text outputs in
  `reviews/evidence/2026-09-05/`; they are frozen evidence, not tests of future fixes.
  Kept binaries, module caches, and screenshots outside version control.
- User explicitly requested commit/push and pre-clear logging, with fixes next session.
  Synced the state digest, task list, and active blocker plan. Application source,
  preferences, daemon configuration, and release channels were not changed.
- At pre-clear close, gracefully quit the temporary review build and relaunched
  `/Applications/syncthingStatus.app`; the returned executable path confirms the
  installed app is restored.

### Later planning follow-up

- User requested project status, then expansion of the implementation plan, explicitly
  authorizing Sol/Luna assistance. Both agents inspected production code; Luna also
  checked the draft. No implementation was requested or started.
- Expanded `IMPLEMENTATION_PLAN.md` into 27 tasks across seven waves: cleanup safety,
  status semantics, refresh ownership, controls/recovery, UI/accessibility, separately
  deferrable maintenance, and integration/release readiness. Each task names targets,
  dependencies where relevant, completion criteria and validation.
- Replaced the single cleanup sprint placeholder with five actionable tasks (0/5).
  The 11 confirmed backlog items and two unconfirmed observations remain queued.
- Planned hostless production tests with isolated filesystem/defaults/credentials/
  transport effects; copied review harnesses remain reproduction evidence only.
  Current project has no test target/shared test scheme; task 1.1 creates them.
- Checked the official Syncthing folder PATCH, needed-item pagination and folder-status
  contracts, with links in the plan. Kept the offline timeout hypothesis unconfirmed.
- Validation: `git diff --check` passed; a document check confirmed 27 unique task IDs,
  seven wave counts, the five sprint IDs and existing local document-link targets.
  No application build, test suite, runtime mutation or release ran in this follow-up.
- Updated project state and prepared this pre-clear handoff. Planning changes and this
  log update remain uncommitted; no commit/push was requested in this follow-up.

## Decisions

- Planned isolated hostless production regressions before cleanup repair; rationale
  and alternatives are recorded in `decisions.md`. This remains unimplemented.

- Homebrew uses this repository as a custom tap, with a pinned checksum for the
  same notarized DMG already hosted on GitHub. Sparkle remains enabled.
- Every public app release updates GitHub release assets, Sparkle appcast metadata,
  Homebrew cask metadata, and the deployed website. Publish the DMG before making
  update feeds point to it. These steps are manual until automated.
- Ordinary code/documentation edits can be committed and pushed without publishing
  a new app release. Homebrew needs no separate binary upload: pushing the updated
  cask to this repository publishes its version/checksum change.

## Next

- Start with `IMPLEMENTATION_PLAN.md` task **1.1** on a local fix branch: establish a
  hostless test target/shared scheme and isolate production dependencies. Then task
  1.2 captures cleanup failures and 1.3 fixes target identity. Do not start with polish.
- Then address status semantics, refresh ownership, notification selection, and the
  remaining confirmed review queue. Measure the offline-peer request-duration cause.
- Complete the two deferred Homebrew checks recorded in `TASKS.md`.
- For the next public version, follow the release checklist and verify all channels.
- Website session: consume the Homebrew handoff, add the installation option to
  the app detail page, deploy, and verify live.

## Resume

- **Publication check:** canonical origin is the public `Xpycode/syncthingStatus`
  repository; authenticated access is ADMIN. Replaced the live peer-name example
  with a placeholder and generalized live counts before publication. Screenshots,
  accessibility dumps, credentials, real sync-folder paths, and folder/device IDs
  are not part of the handoff payload; named filesystem fixtures are synthetic.

- **User instruction:** planning and pre-clear logging only in this follow-up; fixes
  remain next-session work. No half-done application edit or source fix exists.
- **Pickup:** `IMPLEMENTATION_PLAN.md` task **1.1**, then **1.2–1.5**. Compile actual
  production code in hostless tests with isolated stores/transport; do not start the
  real app lifecycle. Cleanup fault chain remains `grantAccess` → `probeFolderAccess`
  → `performDeletion` → `validatePath` in `01_Project/syncthingStatus/Client.swift`.
- **Work state:** six uncommitted documentation files on `main`: `IMPLEMENTATION_PLAN.md`,
  `PROJECT_STATE.md`, `TASKS.md`, `decisions.md`, this log and `sessions/_index.md` (all
  under `docs/`). Preserve these changes before starting a fix branch. No commit,
  push or remote fetch was performed in this planning/log follow-up; locally recorded
  upstream matched main at entry. Prior review publication is separate historical work.
- **Deferred counts:** one active cleanup release blocker; 11 confirmed Backlog tasks
  (nine review follow-ups and two Homebrew checks); five unchecked cleanup sprint tasks
  decompose that one blocker; two Inbox observations awaiting
  reproduction (specific offline-peer latency and disconnected first-run sizing).
  Older feature ideas remain indexed in the state digest.
- **Environment (recorded by the earlier review, not rechecked in planning):** M1-Max,
  Xcode 26.6. Installed app restored at
  `/Applications/syncthingStatus.app`. The Debug build, screenshots, and detailed AX
  dumps remain temporary/local and may disappear; reproduction sources/results are
  versioned. New fixes need a fresh build and their own validation.
- **Validation limits:** no live deletion/configuration mutation, clean-defaults UI
  test, full VoiceOver session, dark appearance test, release cut, or coverage run.
  Frozen harness success means the reviewed defect reproduced, not that it is fixed.
- **Index housekeeping:** pre-existing missing `2026-07-03.md` index entry was already
  documented earlier today; retain that known gap rather than backfill unrelated
  historical work. Today's entry is updated.
- **Model fit:** deep capability + high reasoning for isolating production side effects
  and the following destructive-path work; use Sol/Luna for bounded fixture inspection
  and independent review. Active reasoning setting is not reliably visible.


## Task 1.1 execution follow-up

The user approved execution of the detailed task 1.1 plan. Created
`fix/isolated-production-tests`, preserving the six pre-existing dirty documentation
files. Added the hostless test target/shared schemes, injected settings credentials,
legacy migration and login-item effects, captured client notifications/HTTP, and
injected cleanup bookmark/security-scope/reconciliation boundaries. Production
controller selection, validation and filesystem removal remain real.

Validation: initial validator test passed; six isolated settings tests passed; the
complete 15-test suite passed three consecutive runs with zero failures. Independent
read-only isolation review found no blockers. Debug build and strict signature check
passed; signed App Sandbox entitlement remains enabled. Gracefully quit the installed
app, confirmed exit, and launched exactly one fresh instance from
`/private/tmp/syncthingStatus-v17-build/Build/Products/Debug/syncthingStatus.app`.

[Durable evidence](../reviews/evidence/2026-09-05/task-1.1-isolation.md) includes
coverage boundaries and links to preserved result summaries. Full logs/xcresults stay
under `/private/tmp`. Xcode validation required execution outside the Codex filesystem
sandbox for its services; no tests were skipped. The only matched build warning was
skipped AppIntents metadata extraction.

Implementation checkpoints and new task-specific evidence are committed on the local
branch; prior modified documentation files are preserved and updated but left unstaged.
No push, merge or publication. This follow-up supersedes the earlier planning-only
resume block: sprint is now 1/5, task 1.2 is next. The wrong-root cleanup release blocker
remains open; no live cleanup or daemon configuration mutation was performed.

## Pre-clear handoff

- Uncommitted work: six documentation/session files from the planning and execution updates remain modified on `fix/isolated-production-tests`; implementation checkpoints and task evidence are committed. No application code is half-done.
- Next pickup: task 1.2 in `IMPLEMENTATION_PLAN.md` — add production-controller regressions for ancestor-root and obsolete-identity deletion, using the isolated fixture foundation. Keep live cleanup disabled until the safety gate passes.
- Deferred: 11 confirmed backlog items and two Inbox observations (offline-peer latency and disconnected first-run sizing). Homebrew audit still awaits Xcode 27.0; Xcode 26.6 is installed.
- Model fit: deep capability + high reasoning for destructive-path identity and concurrency work; the active model setting is not reliably visible.
- Test app cleanup: the temporary Debug build was gracefully quit; `/Applications/syncthingStatus.app` was relaunched. The shell `ps` check was blocked by macOS process permissions, so no independent process-list confirmation was available after relaunch.
